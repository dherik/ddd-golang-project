{
    "version": "0.2.0",
    "configurations": [
        {
            "name": "Launch Main App with Debug",
            "type": "go",
            "request": "launch",
            "mode": "debug",
            "program": "${workspaceFolder}"
        }
    ]
}